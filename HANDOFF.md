# Endfield-IS 项目交接文档

> 面向后续接手本项目的模型或开发者。开始工作前必须完整阅读根目录的 `AGENTS.md`；该文件记录了经过用户确认的产品、算法、交互与视觉约束，优先级高于历史设计记录和一般性建议。

## 1. 当前状态

| 项目 | 当前值 |
|---|---|
| 仓库 | [Socialist-Sister/Endfield-IS](https://github.com/Socialist-Sister/Endfield-IS) |
| 分支 | `main` |
| 当前版本提交 | 以 `git rev-list -n 1 v0.2.0` 查询发布提交 |
| Release | [v0.2.0 · 计算核验、性能与体验优化](https://github.com/Socialist-Sister/Endfield-IS/releases/tag/v0.2.0) |
| 主站 | [EdgeOne](https://www.endfieldis.dpdns.org/) |
| 备用站 | [Vercel](https://endfield-is.vercel.app/) |
| 技术栈 | React 19、Vite 6、原生 Web Worker、Canvas、Node.js 原生测试 |
| 后端 | 无；目前所有输入、计算、分享与图片导出均在浏览器本地完成 |

本文件随 v0.2.0 更新。审查基线为 `f5d7e71`（v0.1.3），本轮发布已获用户明确授权。接手时以 `git status`、远端分支和 Release 标签确认实际状态。

v0.2.0 修复跨设施技能切换、少干员分配、Worker 竞态、分享失败及导出裁切；挂机计算热点优化，补充响应式和键盘可访问性。验证为 73 项测试、生产构建、Sites 4 项和官方 npm audit 0 漏洞。详见 [完整审查](docs/reviews/2026-09-05.md) 与 [计算核验](docs/reviews/calculation-v0.2.0.md)。制造批次/时长、类别内培养耗时和线索基础周期仍有实测限制，不要把数学测试通过描述为所有游戏产量准确。

## 2. 产品定义

Endfield-IS 是《明日方舟：终末地》帝江号基建排班计算器。用户手动选择：

- 已拥有的干员；
- 每名干员的 E0–E4 练度；
- 两个制造舱的固定生产配方；
- 培养舱的固定材料类别；
- 长期挂机或固定上线换班模式；
- 挂机模式的启动轴求解范围，或固定换班的每日上线时间。

工具在本地搜索排班并输出五个设施的人员安排、启动/换班时间、技能生效状态和预计日产量。

它不是游戏助手或账号同步工具，不读取、不识别、不控制游戏客户端。

## 3. 用户已经确认的核心产品方向

以下是必须保持的高层方向；更完整、可执行的细则见 `AGENTS.md`。

1. **两步单屏流程**：配置与结果二选一显示，计算成功后进入结果页，“修改配置”返回配置页。
2. **结果快照留存**：配置变化不会让上一次成功结果失效；只有下一次成功计算才替换结果及其输入快照。
3. **两种真实模式**：
   - 长期挂机：模拟自动离岗、恢复与返岗，并搜索启动偏移；
   - 定点换班：每个上线时间执行整组替换，跨班次、跨天连续追踪心情。
4. **五个设施固定顺序**：总控中枢、会客室、制造舱Ⅰ、制造舱Ⅱ、培养舱Ⅰ。
5. **当前干员名单为 30 人**：包括提弗洛斯，排除两种管理员形态。
6. **纯前端分享**：
   - 配置写入 URL hash；
   - 排班图由 Canvas 本地生成；
   - 不上传用户配置或结果。
7. **视觉方向**：终末地官网为主要参考，游戏系统截图为次要参考；黑、雾白、信号黄，方角技术面板、细规则线、克制动效。

## 4. 目录与职责

| 文件/目录 | 职责 |
|---|---|
| `src/App.jsx` | 主界面、配置状态、结果快照、Worker 生命周期、分享与导出入口、结果组件 |
| `src/styles.css` | 全部页面视觉、响应式规则、顶部栏、侧栏、配置与结果布局 |
| `src/operatorData.js` | 30 名干员、头像路径、基建技能、E0–E4 解锁和升级数据 |
| `src/assignmentOptimizer.js` | 跨设施唯一分配、挂机与固定换班候选班组优化 |
| `src/scheduleModel.js` | 心情与生产模拟、启动轴求解、日产量汇总、配方与材料常量 |
| `src/calculation.worker.js` | 将长时间求解移出主线程，向 UI 回传进度和结果 |
| `src/resultCardLayout.js` | 导出文字测量、换行与动态行高的纯布局计算 |
| `src/shareTools.js` | 配置 hash 编解码、canonical URL、排班结果 PNG Canvas 导出 |
| `public/operators/*.webp` | 本地 160×160 方形干员头像；运行时禁止热链第三方头像 |
| `tests/schedule-model.test.mjs` | 算法、排班、心情、配方、产量与名单回归测试 |
| `tests/calculation-oracle.test.mjs` | 独立闭式解与区间积分交叉验证 |
| `tests/result-card-layout.test.mjs` | 班次数量、成员数量和文字长度边界 |
| `scripts/benchmark.mjs` | 可复现的本机两模式性能基准 |
| `tests/share-tools.test.mjs` | 分享配置编解码与 canonical URL 测试 |
| `tests/sites-worker.test.mjs` | Sites 静态资源、SPA fallback 和交付文件测试 |
| `worker/index.js` | Sites 部署的静态资源与 SPA 路由 Worker |
| `scripts/prepare-sites-build.mjs` | 构建后生成 Sites 所需的 server 与 hosting 文件 |
| `vercel.json` | Vercel 构建目录和 SPA rewrite |
| `.openai/hosting.json` | Sites 托管配置；当前不使用 D1 或 R2 |
| `README.md` / `README.en.md` | 中英文对外文档，结构必须同步 |
| `AGENTS.md` | 用户长期确认的项目规则，是后续工作的最高优先级项目上下文 |

### 历史文件注意事项

`design-qa.md` 是早期重设计阶段的历史记录，其中仍有“29 名干员”“以符号代替头像”等过时文字，也引用了一些已经整理或删除的本地截图。它可以用于理解设计演进，但**不能作为当前产品事实来源**。当前事实以代码、测试、README 和 `AGENTS.md` 为准。

## 5. 当前界面与交互

### 桌面结构

- 顶部栏高度与左侧栏宽度使用同一 token，左上形成严格正方形交点。
- 顶部栏为浅色；左侧栏为石墨黑；当前步骤仅使用窄黄标记和黄图标。
- 顶部中文名称在前，英文 `// DIJIANG / SHIFT CALCULATOR` 在右侧同一基线。
- 窄屏隐藏英文标题，而不是将其堆叠到中文标题上方。
- 侧栏顺序：配置、结果、关于。

### 配置页

- 标题为“基建配置”，副文案为“选择策略、产出与干员”。
- 主操作“生成排班结果”位于页面标题区域。
- “复制配置链接”与主操作并列。
- 干员工具栏显示 `已选 N/30` 与“全选 30 / 全不选”。
- 所有干员初始不勾选，但练度默认 E4。
- 全选操作始终针对完整 30 人名单，不受搜索或设施筛选影响。
- 左侧条件面板与右侧干员面板上下严格对齐，右侧列表在匹配高度内滚动。

### 结果页

- 标题为“计算结果”。
- 指标大数字只显示数值，“日均”作为独立小单位。
- 指标注释使用“设施 · 等级 · 计算依据”的顺序。
- 不显示“结果可用”“减耗组”“无减耗组”或独立启动轴摘要。
- 挂机结果中，每名干员卡上方独立显示 `T 0h / T +2h / …` 时间轨。
- 固定换班结果按班次横向分列，每个班次内将干员显示为横向整行卡片。
- 每张干员卡保留两条技能；当前设施/配方生效的技能优先，其他技能灰显。

### 关于页

配置和结果页只保留可操作输入与计算输出。算法口径、限制、仓库链接等集中在“关于”页，不要重新把大段说明塞回配置或结果页。

## 6. 当前计算模型

### 基础规则

- 工作设施心情消耗：`7% / 小时`。
- 总控中枢基础恢复：`12% / 小时`，再乘算生效的恢复技能。
- 制造、培养、会客每个时间片效率：

  ```text
  (1 + 40% × 当前在岗人数) × (1 + 匹配技能加成总和)
  ```

- 无匹配技能的干员仍提供通用 40% 进驻加成。
- 设施无人时不生产。
- 模拟默认从满心情开始；当前不要求用户输入实时心情。

### 长期挂机

- 干员心情归零后离岗，在总控恢复到满心情后返岗。
- 启动偏移以 30 分钟为粒度搜索。
- 可选统一启动轴或各设施独立启动轴。
- 长时间预热后统计稳定周期。
- 优化目标是计入停工后的实际长期日产量，不把 100% 覆盖率设为硬约束。
- 分设施方案只有在验证日产量超过统一方案时才可宣称更优。
- 总控以外的设施不能混合“心情消耗降低组”与普通组，避免周期漂移。

### 固定上线换班

- 每个用户输入的时间都是独立换班节点。
- 每个节点执行整组替换。
- 心情跨班次、跨天连续追踪，禁止将复用干员重置为满心情。
- 允许停工或复用的唯一理由是长期产量确实更高。

### 制造与培养

- 制造舱Ⅰ、Ⅱ各自固定一个配方：
  - 高级认知载体；
  - 高级作战记录；
  - 武器检查套组。
- 默认分别为高级认知载体和高级作战记录。
- 两个制造舱选择相同配方时，结果汇总该产品日产量。
- 培养舱整次模拟固定为矿物、晶植或菌类质料，默认矿物质料。
- 培养结果只表达材料类别，不暗示每个类别只有一种具体物品。

### 会客室

- 特定线索技能只保留 L1/L2 定性倾向。
- 相同特定线索效果不叠加。
- 不增加未经验证的固定每日线索数。

## 7. 干员数据维护

### 当前名单

- 共 30 名。
- 最新加入：提弗洛斯（`typhoeus`）。
- 提弗洛斯数据：
  - 晶石低语：E1 矿物质料培养 +20%，E3 升至 +30%；
  - 孢子生灵：E2 菌类质料培养 +20%，E4 升至 +30%。
- 管理员两种形态均排除。

### 添加新干员时

1. 找到可交叉验证的基建技能、解锁阶段与数值，不要猜测。
2. 在 `src/operatorData.js` 添加数据。
3. 将方形头像保存为 `public/operators/<id>.webp`，尽量统一为 160×160。
4. 更新名单数量相关测试、README 徽章/文案和 `AGENTS.md`。
5. 搜索所有旧计数：

   ```bash
   rg -n "30|N/30|Operators-30" src tests README.md README.en.md AGENTS.md
   ```

6. 运行完整测试和浏览器检查。

注意：代码中的像素数值如 `29px` 与名单数量无关，不要机械替换。

## 8. 分享与图片导出

### 配置分享

- canonical URL：`https://www.endfieldis.dpdns.org/`。
- 入口位于配置页标题操作区。
- 只编码手动输入，写入 URL hash，不使用查询参数或后端。
- 解码时会清洗未知干员、非法练度、非法配方、重复/非法时间。

### PNG 导出

`src/shareTools.js` 使用 Canvas 在浏览器本地绘制。

`v0.1.3` 重构导出结构，`v0.2.0` 补充动态文字测量、成员行高和重复导出保护：

- 导出图不再复制网页的“02 / 计算结果 / RESULT”标题；
- 使用专属黑底导出抬头：
  - 左侧信号黄计算器识别块；
  - 中部“帝江排班方案”；
  - 右侧方案类型与生成日期；
- 保留五项日产量指标和四项运行指标；
- 挂机模式按三列启动时间轨显示；
- 固定换班模式按班次分列；
- 每名干员包含本地头像、姓名和两条技能；
- 非当前设施或配方生效的技能灰显；
- 设施行根据班次人数自动增高，禁止恢复为固定 148px 高度。

导出设计的重点是：图片脱离网页后仍是一张完整、可发布的排班方案，而不是网页截图或拥挤表格。

## 9. 视觉与实现禁区

除非用户明确推翻，不要：

- 大范围改动已经确认的页面结构；
- 增加独立设施主轴或重复设施摘要栏；
- 使用泛化 AI 仪表盘式圆角卡片、渐变和装饰图形；
- 添加没有计算功能的英雄图；
- 把配置与结果同时放在一个页面；
- 把英文标题堆到中文标题上方；
- 加回右上角页码/状态；
- 加回大块黄色侧栏按钮；
- 加回“仅本地”“演示数据”等持久免责声明；
- 声称工具能检测账号、读取心情或控制游戏；
- 热链远程干员头像；
- 为线索概率、实时状态或未知基建数据编造精确值。

做耐久性的视觉变更时，将用户确认的新规则写入 `AGENTS.md`。

## 10. 本地运行与验证

需要 Node.js 20 或更高版本。

```bash
npm install
npm run dev -- --host 127.0.0.1
```

按照项目指令，应由接手模型自行启动本地服务器，并在可用浏览器中打开预览，不要只给用户启动说明。

### 完整验证

```bash
npm test
npm run build
npm run test:sites
```

或一次运行全部测试：

```bash
node --test tests/*.test.mjs
```

构建成功后必须存在：

- `dist/client/index.html`
- `dist/server/index.js`
- `dist/.openai/hosting.json`

### 浏览器验收建议

至少检查：

1. 桌面配置页，无横向溢出。
2. 选择全部 30 人，练度选择与两条技能显示正确。
3. 长期挂机计算可完成并进入结果页。
4. 返回配置修改条件后，上一次结果仍可进入且保持旧输入快照。
5. 固定上线模式在两个以上时间点可生成不同班次。
6. 复制配置链接后，新页面能还原输入。
7. 导出的挂机图和固定换班图无裁切、重叠或缺失头像。
8. 390×844 左右的窄屏没有底栏遮挡和 body 横向滚动。
9. 浏览器控制台无新增错误和资源 404。

## 11. 部署与发布

### 自动部署

EdgeOne Makers 与 Vercel 均跟踪 GitHub `main`：

| 平台 | 安装 | 构建 | 输出 |
|---|---|---|---|
| EdgeOne Makers | `npm install` | `npm run build` | `dist/client` |
| Vercel | `npm install` | `npm run build` | `dist/client` |

EdgeOne 自定义域名是 canonical 主站；Vercel 是海外/备用入口。

### 发布惯例

当前 Release 采用语义化标签：

- `v0.1.0`
- `v0.1.1`
- `v0.1.2`
- `v0.1.3`
- `v0.2.0`

发布前：

1. 更新中英文 README 中的当前版本标记。
2. 完成测试、生产构建和 `git diff --check`。
3. 确认没有临时截图、预览 HTML 或构建产物进入提交。
4. 提交并推送 `main`。
5. 使用 `gh release create <tag> --target main ...` 创建正式 Release。
6. 验证本地 HEAD、`origin/main` 与 Release 标签指向同一提交。

`package.json` 当前仍使用 `0.0.0`，项目实际版本以 Git tag 和 GitHub Release 为准。除非决定建立包版本同步流程，不要只改该字段造成另一套版本来源。

## 12. 已知限制与技术债

1. **无持久化后端**：刷新后不保存历史计算；分享依赖 URL hash。当前这是有意的零成本设计，不是缺陷。
2. **系统字体差异**：导出 Canvas 使用 Bahnschrift、微软雅黑等系统字体；不同平台可能有轻微字宽差异。
3. **数据更新依赖人工核验**：游戏版本更新后需重新核对干员基建技能、配方时长和机制。
4. **产量口径待实测**：线索 72 小时基础周期、制造批次/效率解释仍需核验；培养类别统一按 61:06:40 折算，不能代表同类其他耗时材料。定向线索只做定性倾向。
5. **初始心情固定**：当前按满心情开始模拟，尚未提供手动初始心情输入。
6. **自动化视觉测试有限**：算法与路由有测试，Canvas 导出和响应式布局仍主要依赖浏览器截图验收。
7. **历史 QA 文档过时**：`design-qa.md` 应在下次系统性视觉 QA 时重写或归档，不能直接当作当前验收结论。
8. **README 截图可能滞后**：每次明显视觉更新后，应重新生成 `docs/images/interface-config.png` 与 `interface-result.png`，并同步中英文说明。

## 13. 推荐的后续工作顺序

如用户没有指定具体任务，建议按以下顺序推进：

1. 用当前游戏内生产任务核验制造每批数量、基准效率、培养不同耗时和线索基础周期，再同步转换公式及独立测试。
2. 补充 Safari/iOS 导出与字体验收；v0.2.0 已完成 Chromium 流程、响应式检查和纯布局测试。
3. 检查最新游戏版本是否还有新增或调整的基建数据。
4. 根据真实用户反馈补充功能，不主动引入账号系统或后端。
5. 若未来确需跨设备保存历史方案，再评估可选后端；在此之前保持纯前端和零成本部署。

## 14. 接手模型的最短启动清单

1. 完整阅读 `AGENTS.md` 和本文件。
2. 运行 `git status --short`，不要覆盖用户或上一模型的未提交修改。
3. 阅读与任务直接相关的源文件和测试。
4. 启动本地服务器并打开浏览器预览。
5. 保持既有操作逻辑，除非用户明确要求改变。
6. 视觉变更必须实际截图检查；算法变更必须新增或更新测试。
7. 不推送、不发布、不部署，除非用户明确授权。
8. 交付时说明修改文件、验证结果、是否已提交/推送。

---

一句话交接：**这是一个已经上线的纯前端帝江号排班计算器；当前重点是保持已确认的黑白黄工业视觉与两步操作逻辑，在可验证的游戏数据基础上迭代算法、名单和分享输出，不要重新走回泛化 AI 仪表盘或未经证实的游戏机制。**
